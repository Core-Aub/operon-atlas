[project]
name = "operonatlas-worker"
version = "0.1.0"
description = "Python Cloudflare Worker API for OperonAtlas"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
]
